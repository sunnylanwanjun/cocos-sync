import { TextureCube, _decorator, __private } from 'cc';
import { Cubemap } from './cubemap';

const { ccclass, type, property } = _decorator;

@ccclass('sync.SkyLight')
export class SkyLight extends Cubemap {
}
